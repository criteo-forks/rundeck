---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: centos-6.5

suites:
  - name:           server-ldap
    data_bags_path: 'test/integration/data_bags'
    run_list:
      - 'recipe[rundeck::server]'
    attributes:
      jaas: 'activedirectory'
